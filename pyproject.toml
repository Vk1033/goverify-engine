[project]
name = "goverify-engine"
version = "0.1.0"
description = "GoVerify Engine - Identity Verification Platform"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv]
workspace = { members = ["ai-service"] }
